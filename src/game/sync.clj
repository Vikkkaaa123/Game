(ns game.sync
  "Простая синхронизация")

(defn sync-all-rooms []
  (println "[SYNC] Комнаты синхронизированы (заглушка)"))

(defn sync-all-players []
  (println "[SYNC] Игроки синхронизированы (заглушка)"))
